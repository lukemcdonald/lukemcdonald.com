# Agent notes

Sound is off by default (`DEFAULT_SOUND_PREFERENCE = 'off'`).

## No sound effects on mobile

Do not add sound effects, a Sound row, or tap-cue workarounds on the phone menu or other coarse-pointer UI.

Cuelume hover is mouse-only. That silence on tap is accepted. Do not pair hover with press/toggle on destinations to “fix” phones.

Desktop Cmd+K may keep its existing sound toggle. Leave phone chrome as Theme and Appearance only.

Closed attempts: #68 (phone Sound row) and #69 (pointer-aware link cues).
