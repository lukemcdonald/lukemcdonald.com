---
import type { EducationItem } from '@/features/resume/resume.types'

import { Brain as EducationIcon } from 'lucide-astro'

import Section from '@/features/resume/components/ui/Section.astro'

export interface Props {
  education: EducationItem[]
}

const { education } = Astro.props
---

<Section
  title="Education"
  items={education}
  icon={EducationIcon}
>
  <template slot="item">
    {
      (item: EducationItem) => (
        <>
          {item.studyType}
          {item.area ? `${item.area} ` : ''}— {item.institution}
        </>
      )
    }
  </template>
</Section>
